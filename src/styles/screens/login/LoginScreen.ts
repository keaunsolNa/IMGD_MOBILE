import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    padding: 24,
  },
  buttonContainer: {
    marginBottom: 12,
  },
  loadingContainer: {
    marginTop: 12,
  },
});
